/* Runtime reliability: session restoration, inactivity timeout and synchronized ticket data. */
(function(){
  const SESSION_KEY='onomo_active_session_v1', ACTIVITY_KEY='onomo_last_activity_v1', INACTIVITY=15*60*1000;
  let syncTimer=null, channel=null, realtimeChannel=null, realtimeClient=null, loading=false;
  const t=key=>window.OnomoI18n?.t(key)||key;
  const writeSession=()=>currentUser&&localStorage.setItem(SESSION_KEY,JSON.stringify({id:currentUser.id,email:currentUser.email,at:Date.now()}));
  const clearSession=()=>{localStorage.removeItem(SESSION_KEY);localStorage.removeItem(ACTIVITY_KEY);};
  const touch=()=>{if(currentUser)localStorage.setItem(ACTIVITY_KEY,String(Date.now()));};
  function refreshBadge(){if(!currentUser)return;const all=visibleTickets();const badge=document.getElementById('sbOpen');if(badge)badge.textContent=all.length;}
  function refreshView(){if(!currentUser)return;updateCounts();refreshBadge();if(['dashboard','tickets','urgents','my-tickets','detail'].includes(currentView))renderView();}
  async function syncTickets(){if(loading||!currentUser||!sbOK())return;loading=true;try{const previous=new Map(tickets.map(ticket=>[ticket.id,ticket]));const records=await sbLoadTickets();if(Array.isArray(records)){tickets=records;saveTickets(tickets);records.forEach(ticket=>{const before=previous.get(ticket.id),mine=(ticket.assigne_a===`${currentUser.prenom} ${currentUser.nom}`.trim());if(before&&mine&&before.assigne_a!==ticket.assigne_a)window.addNotif?.(`Ticket ${ticket.numero} assigné à votre équipe.`,'ticket','var(--brand)');if(before&&mine&&before.statut!==ticket.statut&&ticket.statut==='fermé')window.addNotif?.(`Le ticket ${ticket.numero} a été fermé.`,'circle-check','var(--green)');});refreshView();}}catch(error){console.warn('Ticket sync unavailable',error);}finally{loading=false;}}
  function startRealtime(){if(realtimeChannel||!window.supabase||!sbOK())return;try{realtimeClient=window.supabase.createClient(settings.sbUrl,settings.sbKey,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});realtimeChannel=realtimeClient.channel('onomo-ticket-events').on('postgres_changes',{event:'*',schema:'public',table:'tickets'},()=>syncTickets()).on('postgres_changes',{event:'*',schema:'public',table:'commentaires'},()=>syncTickets()).subscribe();}catch(error){console.warn('Supabase Realtime indisponible : recours à la synchronisation périodique.',error);}}
  function startSync(){if(syncTimer)clearInterval(syncTimer);syncTickets();startRealtime();syncTimer=setInterval(syncTickets,10000);}
  async function restoreSession(){try{if(window.supabase&&sbOK()){const client=window.supabase.createClient(settings.sbUrl,settings.sbKey,{auth:{persistSession:true,autoRefreshToken:true}});const {data:{session}}=await client.auth.getSession();if(session?.user){const rows=await sbFetch(`utilisateurs?auth_user_id=eq.${session.user.id}&limit=1`);if(rows?.[0]){currentUser=dbRowToUser(rows[0]);initSession();return;}}}const saved=JSON.parse(localStorage.getItem(SESSION_KEY)||'null');if(!saved||currentUser)return;const user=DEMO_USERS.find(item=>item.id===saved.id&&item.email===saved.email);if(user){currentUser=user;initSession();}}catch(error){console.warn('Restauration de session impossible',error);}}
  const previousInit=window.initSession;window.initSession=function(){previousInit();writeSession();touch();startSync();};
  const previousLogout=window.doLogout;window.doLogout=function(){clearSession();if(syncTimer)clearInterval(syncTimer);return previousLogout();};
  const previousUpdate=window.updateTicket;window.updateTicket=async function(id,updates){const normalized={...updates};if(normalized.statut==='résolu')normalized.statut='fermé';const result=await previousUpdate(id,normalized);touch();channel?.postMessage({type:'tickets-updated'});await syncTickets();return result;};
  const previousCreate=window.createTicket;window.createTicket=async function(data){const result=await previousCreate(data);touch();channel?.postMessage({type:'tickets-updated'});await syncTickets();return result;};
  const previousComment=window.addComment;window.addComment=async function(message){if(currentTicket?.statut==='fermé'&&['admin','it_regional','it_hotel'].includes(currentUser?.role)){await updateTicket(currentTicket.id,{statut:'nouveau'});}const result=await previousComment(message);touch();channel?.postMessage({type:'tickets-updated'});await syncTickets();return result;};
  const previousSwitch=window.switchView;window.switchView=function(view,element){touch();return previousSwitch(view,element);};
  document.addEventListener('DOMContentLoaded',()=>{['click','pointermove','keydown','input','submit','touchstart'].forEach(type=>document.addEventListener(type,touch,{passive:type==='pointermove'||type==='touchstart'}));try{channel=new BroadcastChannel('onomo-ticket-sync');channel.onmessage=event=>{if(event.data?.type==='tickets-updated')syncTickets();};}catch(_){/* polling continues when BroadcastChannel is unavailable */}restoreSession();setInterval(()=>{const last=Number(localStorage.getItem(ACTIVITY_KEY)||0);if(currentUser&&last&&Date.now()-last>=INACTIVITY){clearSession();showToast('Votre session a expiré pour cause d’inactivité.','err');doLogout();}},15000);});
  window.OnomoRuntime={syncTickets,touch,refreshBadge};
})();
